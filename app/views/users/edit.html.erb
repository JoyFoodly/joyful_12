<div class="bg-grey general-padding">
  <div class="row">
    <div class='col-md-8'>
      <h3 class="no-buffer">Your Joyful 12 profile</h3>
      <p>All responses are optional and private. Use this profile to help you track your family's eating habits.</p>
    </div>
    <div class='col-md-4'>
      <p>
        <%= link_to 'Head on over to Orientation', page_path('orientation'), class: 'btn btn-primary btn-lg' %>
        <img src="http://d3uroln2zt6ogz.cloudfront.net/pages/media/original/1af5186ee32c93a86c7c20c6634fb256bceba7e6.png?1393877240" />
      </p>
    </div>
  </div>

  <% if @user.errors.present? %>
    <div class='alert alert-danger'>
      <a class="close" data-dismiss="alert">×</a>
      <%= @user.errors.full_messages.first  %>
    </div>
  <% end %>

  <%= form_for @user, html: { role: "form" } do |f| %>
    <div class='row'>
      <div class='col-md-12'>
        <div class="form-group">
          <%= f.label :username %>
          <%= f.text_field :username, class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.label :email %>
          <%= f.text_field :email, class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.label :first_name %>
          <%= f.text_field :first_name, class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.label :last_name %>
          <%= f.text_field :last_name, class: 'form-control' %>
        </div>
      </div>
    </div>

    <h4 class="top-buffer">Family members participating in the course</h4>
    <%= f.fields_for :family_members do |builder| %>
      <%= render 'family_member_fields', f: builder %>
    <% end %>

    <div class='row'>
      <p>
        <%= link_to_add_fields "Add A Family Member", f, :family_members, class: 'js-add-fields btn btn-primary pull-right' %>
      </p>
    </div>

    <div class="row">
      <div class="col-md-8 col-md-offset-2 bg-white general-padding top-buffer">
        <h3 class="top-buffer text-center no-buffer">Beta Members - keep on the lookout!</h3>
        <p>We'll be adding new features to help you track your family's progress and preferences. What type of information
          would you find useful here? Give us feedback:</p>
        <a class="btn btn-primary centered" href="/pages/general-feedback">Give Feedback</a>
      </div>
    </div>

    <hr>

    <%= f.submit 'Update Profile', class: 'btn btn-primary' %>
    <%= link_to 'change password', change_password_user_path('me') %>
  <% end %>
</div>

<div class="page-header">
  <h3>Saved Shopping Lists</h3>
</div>

<table class='table table-bordered table-striped table-text-middle'>
  <tr>
    <th>Name</th>
    <th>Created On</th>
    <th></th>
  </tr>
  <tr>
    <td><%= link_to 'Current list', edit_shopping_list_path('current') %></td>
    <td></td>
    <td></td>
  </tr>
  <% current_user.shopping_lists.order(created_at: :desc).where.not(completed_at: nil).each do |shopping_list| %>
  <tr>
    <td><%= link_to shopping_list.name, edit_shopping_list_path(shopping_list) %></td>
    <td><%= shopping_list.created_at.strftime('%B %e, %Y') %></td>
    <td>
      <%= link_to "Remove", shopping_list, method: :delete, confirm: 'Are you sure you want to delete this shopping list?',
                                                            class: 'btn btn-sm btn-default' %>
    </td>
  </tr>
  <% end %>
</table>
