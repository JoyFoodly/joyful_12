<div class="row">
  <div class='col-lg-4 col-md-4'>
    <h2>Forgot your password?</h2>
    <p>Have no fear! We'll get you back in the Joyful 12 in no time!</p>

    <%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { method: :post, role: "form" }) do |f| %>
        <%= devise_error_messages! %>

        <div class="form-group">
          <%= f.label :email %>
          <%= f.email_field :email, class: 'form-control', autofocus: true, placeholder: 'Enter email' %>
        </div>

        <%= f.submit "Send me reset password instructions", class: 'btn btn-primary' %>
    <p>And remember, when you're signing in, you can click the “Remember Me” button to avoid having to sign in every time from your home computer.</p>
    <% end %>
  </div>
</div>
<br />
<%= render "devise/shared/links" %>
