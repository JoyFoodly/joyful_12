<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
  <meta name="viewport" content="width=device-width">
  <title>Joyfoodly</title>
  <%= favicon_link_tag 'favicon.ico' %>
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div class="container">

    <header class="masthead">
      <h1><center><%= link_to (image_tag 'joyful12logo.png', class: 'img-responsive'), root_url %></center></h1>
    </header>

    <% flash.each do |name, msg| %>
      <div class="alert alert-<%= name == :notice ? "success" : "danger" %>">
        <%= msg  %>
      </div>
    <% end %>

    <section class='main-content'>
      <%= yield %>
    </section>

    <hr />

    <footer>
      <p>&copy; JoyFoodly <%= Date.current.year %></p>
    </footer>

  </div>


<%= render 'layouts/jquery_with_cdn_fallback' %>
<%= render 'layouts/tracking' %>
<%= javascript_include_tag 'application' %>

</body>
</html>
